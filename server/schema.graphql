type Query {
  user(id: Int!): User!
  users: [User!]!
  photo(id: Int!): Photo!
  photos: [Photo!]!
}

type User {
  id: Int!
  name: String!
  email: String!
  website: String!
}

type Photo {
  id: Int!
  title: String!
  url: String!
}
